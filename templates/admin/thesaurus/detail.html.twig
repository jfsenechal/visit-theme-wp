<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URN Detail — {{ urn }}</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; background: #f5f5f5; color: #333; padding: 2rem; }
        h1 { margin-bottom: 0.25rem; font-size: 1.3rem; }
        .urn-code { font-family: monospace; color: #0056b3; font-size: 1rem; margin-bottom: 1.5rem; display: block; }
        .back { margin-bottom: 1.5rem; display: inline-block; color: #0056b3; text-decoration: none; }
        .back:hover { text-decoration: underline; }
        table { background: #fff; border: 1px solid #ddd; border-radius: 6px; border-collapse: collapse; width: 100%; max-width: 600px; }
        th, td { padding: 0.6rem 1rem; text-align: left; border-bottom: 1px solid #eee; }
        th { background: #e9ecef; font-weight: 600; width: 80px; }
        td { font-size: 0.95rem; }
        .empty { color: #999; font-style: italic; }
    </style>
</head>
<body>
    <a href="" class="back">&larr; Back to browser</a>

    <h1>URN Detail</h1>
    <span class="urn-code">{{ urn }}</span>

    {% if labels|length > 0 %}
        <table>
            <thead>
                <tr>
                    <th>Lang</th>
                    <th>Label</th>
                </tr>
            </thead>
            <tbody>
                {% for label in labels %}
                    <tr>
                        <td><strong>{{ label.lang }}</strong></td>
                        <td>{{ label.value }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p class="empty">No labels found for this URN.</p>
    {% endif %}
</body>
</html>
