{% set  data =  specs['urn:cat:accueil'] ?? [] %}
{% if data|length > 0 and data.items > 0 %}
    {% include '@Visit/offre/_specification_line.html.twig' %}
{% endif %}

{% set  data =  specs['urn:cat:eqpsrv'] ?? [] %}
{% if data|length > 0 and data.items > 0 %}
    {% include '@Visit/offre/_specification_line.html.twig' %}
{% endif %}

{% set  data =  specs['urn:cat:classlab'] ?? [] %}
{% if data|length > 0 and data.items > 0 %}
    {% include '@Visit/offre/_specification_line.html.twig' %}
{% endif %}

{% set  data =  specs['urn:cat:tarif'] ?? [] %}
{% if data|length > 0 and data.items > 0 %}
    {% include '@Visit/offre/_specification_line.html.twig' %}
{% endif %}


{# % include '@Visit/offre/_all_specifications.html.twig' % #}
