<div>
    <h4 class="text-lg font-semibold mb-4 text-cta-light">{{ 'discover'|trans }}</h4>
    <ul class="space-y-2">
        {% for icon in icons %}
            <li>
                <a href="{{ icon.url }}" class="text-gray-300 hover:text-white transition-colors">{{ icon.name }}</a>
            </li>
        {% endfor %}
        <li>
            <a href="https://circuit-court.marche.be/"  target="_blank" rel="noopener noreferrer" class="text-gray-300 hover:text-white transition-colors">
                {{ 'local.products'|trans }}
                <span class="sr-only">({{ 'opens.new.window'|trans }})</span>
            </a>
        </li>
    </ul>
</div>