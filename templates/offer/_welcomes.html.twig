{% if offer.welcomeFlags|length > 0 or offer.specificationDetail.openingIndications %}
    {# ── Welcome & Accessibility ──
    {% set activeWelcomeFlags = detail.welcomeFlags|filter(v => v) %}
    {% if activeWelcomeFlags|length > 0 or detail.openingIndications %}
    #}
    {% if offer.welcomeFlags|length > 0 or offer.specificationDetail.openingIndications %}
        <div class="mb-6">
        <h3 class="font-montserrat-semi-bold text-lg text-caractere mb-3 flex items-center gap-2">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                 stroke="currentColor" class="w-5 h-5 text-cta-dark">
                <path stroke-linecap="round" stroke-linejoin="round"
                      d="M15.182 15.182a4.5 4.5 0 01-6.364 0M21 12a9 9 0 11-18 0 9 9 0 0118 0zM9.75 9.75c0 .414-.168.75-.375.75S9 10.164 9 9.75 9.168 9 9.375 9s.375.336.375.75zm-.375 0h.008v.015h-.008V9.75zm5.625 0c0 .414-.168.75-.375.75s-.375-.336-.375-.75.168-.75.375-.75.375.336.375.75zm-.375 0h.008v.015h-.008V9.75z"/>
            </svg>
            {{ 'offer.welcome'|trans|default('Accueil & accessibilité') }}
        </h3>

        <div class="flex flex-wrap gap-2">
            {% for name, active in offer.welcomeFlags %}
                {% if active %}
                    <span
                        class="inline-flex items-center gap-1.5 rounded-md bg-purple-50 px-3 py-1.5 text-sm text-purple-800 ring-1 ring-purple-200/80 ring-inset">
                                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor"
                                     class="w-3.5 h-3.5 text-purple-500">
                                    <path fill-rule="evenodd"
                                          d="M16.704 4.153a.75.75 0 01.143 1.052l-8 10.5a.75.75 0 01-1.127.075l-4.5-4.5a.75.75 0 011.06-1.06l3.894 3.893 7.48-9.817a.75.75 0 011.05-.143z"
                                          clip-rule="evenodd"/>
                                </svg>
                                {{ name }}
                            </span>
                {% endif %}
            {% endfor %}
        </div>
    {% endif %}

    {% if offer.specificationDetail.openingIndications %}
        <p class="mt-3 text-sm text-gray-600">{{ offer.specificationDetail.openingIndications|raw }}</p>
    {% endif %}
    </div>
{% endif %}
