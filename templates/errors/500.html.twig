{% include '@Visit/article/_header_article.html.twig' %}
{% include '@Visit/article/_cadre.html.twig' %}
<main class="container mx-auto">
    <article
            class="grid grid-cols-1 xl:grid-cols-[60%_40%] items-start mt-2 xl:mt-4 mx-auto px-4">
        <div class="p-2 xl:p-4">
            <h1 class="font-montserrat-semi-bold text-lg xl:text-2xl text-caractere">
                Erreur de chargement de la page</h1>
            <h2 class="">La page n'a pas pu s'afficher</h2>
            <section class="my-8 text-body prose">
                <p>Nous allons régler le problème aussi vite que possible</p>
                <p><a href="{{ urlBack ?? '/' }}">Vous pouvez rejoindre la page d'accueil</a></p>
                {% if message is defined %}
                    <p>{{ message }}</p>
                {% endif %}
            </section>
        </div>
    </article>
</main>
